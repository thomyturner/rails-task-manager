<%= form_for(@task) do |t| %>
  <%= t.label :title %>
  <%= t.text_field :title %>

  <%= t.label :details %>
  <%= t.text_field :details %>

  <%#= f.button f.object.new_record? ? 'Create' : 'Update' %>
  <%= t.submit %>
<% end %>
